server.port=8080
spring.application.name = InfoSystem - Atekom doo
jwt.secret = djerman030607300609
baza = atekom
spring.jpa.properties.hibernate.jdbc.time_zone=UTC
spring.datasource.driver-class-name = com.mysql.cj.jdbc.Driver
spring.datasource.url = jdbc:mysql://localhost:3306/${baza}?serverTimezone=UTC&characterEncoding=UTF-8&amp&allowPublicKeyRetrieval=true&verifyServerCertificate=false&useSSL=false&requireSSL=false
spring.datasource.username = atekom
spring.datasource.password = Atekom21!
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL8Dialect
spring.jpa.hibernate.naming.physical-strategy = org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
spring.jackson.serialization.fail-on-empty-beans = false
spring.data.web.pageable.one-indexed-parameters = true
spring.data.web.pageable.default-page-size = 100
spring.data.web.pageable.max-page-size = 1000

#spring.jpa.show-sql = true
#spring.jpa.properties.hibernate.format_sql = true

# Enable multipart uploads
spring.servlet.multipart.enabled=true
# Threshold after which files are written to disk.
spring.servlet.multipart.file-size-threshold=2KB
# Max file size.
spring.servlet.multipart.max-file-size=200MB
# Max Request Size
spring.servlet.multipart.max-request-size=215MB

#upravljanje sesijama
server.servlet.session.cookie.http-only = true
server.servlet.session.cookie.secure = true
spring.session.store-type = jdbc

#spring.datasource.platform = mysql
spring.sql.init.platform = mysql
spring.session.jdbc.initialize-schema = always

#spring.jpa.hibernate.ddl-auto = create-drop
#server.servlet.context-path=/brojObavestenja - promena podesavanja putanje za aplikaciju: ovako se poziva samo na localhost:8080/brojObavestenja
